#include <stdio.h>
int main(){
    // char a = getchar();
    // printf("You just entered %c", a);
    // char b[100];
    // gets(b);
    // printf("Entered: %s",b);
    int e;
    scanf("%d", &e);
    printf("Entered %d just now", e);
    return 0;
}